<UserControl x:Class="cours_mvvm1.MVVM.View.Shipments"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:vm="clr-namespace:cours_mvvm1.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="622"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:ShipmentsMV/>
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="0,20,20,0">
            <Border.Background>
                <ImageBrush ImageSource="/Asset/Icons/shipment_page.png"/>
            </Border.Background>
        </Border>

        <Grid>
            <TextBlock Text="Expected Delivery :" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   Foreground="#FFF" 
                   FontWeight="Bold" 
                   FontSize="15" 
                   Margin="0 0 142 100"/>

            <TextBlock Text="{Binding ShipmentTracking}" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   Foreground="#FFFF00" 
                   FontWeight="Bold" 
                   FontSize="12" 
                   FontFamily="/Asset/Fonts/#Rubik Medium" 
                   Margin="0 0 88 100"/>
        </Grid>
    </Grid>
</UserControl>
