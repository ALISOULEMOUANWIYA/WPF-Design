<Window x:Class="cours_mvvm1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:cours_mvvm1.MVVM.ViewModel"
        xmlns:Menu="clr-namespace:cours_mvvm1.utilities"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="600" 
        Width="850"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent">

    <Window.DataContext>
        <vm:NavigationsMV/>
    </Window.DataContext>

    <Grid>
        <!--// Window Border //-->
        <Border Background="#212529" CornerRadius="20"/>

        <!--// Base Gride //-->
        <Grid>
            <!--// Redimenssion //-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="228"/>
                <ColumnDefinition Width="622"/>
            </Grid.ColumnDefinitions>
            <Grid HorizontalAlignment="Left"
                  Width="228">
                <!--// Nave bar left //-->
                <Border Background="#272B2F"
                        CornerRadius="20 0 0 20">
                    <StackPanel Width="228"
                                Height="400">
                        <!--// Home //-->
                        <Menu:Btn Style="{StaticResource BtnStyle}"
                                  Command="{Binding HomeCommand}"
                                  IsChecked="True">
                            <Grid>

                                <Image Source="/Asset/Icons/img_home.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Home"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>
                        </Menu:Btn>

                        <!--// Customer //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding CustomersCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_customer.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Customer"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>

                        <!--// Products //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding ProductsCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_product.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Products"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>

                        <!--// Orders //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding OrdersCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_order.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Orders"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>

                        <!--// Transactions //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding TransactionsCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_transaction.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Transactions"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>

                        <!--// Shipments //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding ShipmentsCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_shipment.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Shipments"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>

                        <!--// Settings //-->

                        <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding SettingsCommand}">

                            <Grid>

                                <Image Source="/Asset/Icons/img_setting.png"
                                   Style="{StaticResource Image_Style}" />

                                <TextBlock Text="Settings"
                                       Style="{StaticResource Text_Style}" />

                            </Grid>

                        </Menu:Btn>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Column="1">

                <ContentControl x:Name="Pages"
                                Content="{Binding CurrentView}" />

            </Grid>
        </Grid>

        <Button x:Name="CloseApp"
                Style="{StaticResource ExitApp}"
                Click="CloseApp_Click" />
    </Grid>
</Window>
